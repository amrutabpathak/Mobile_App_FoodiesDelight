<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.mad_sea.foodiesdelight">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RestSideViewOrderActivity"></activity>
        <activity android:name=".RestaurantLoginActivity" />
        <activity android:name=".FoodDetailsOrder" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".CartAcitivity" />
        <activity android:name=".RestaurantDetailsActivity" />
        <activity android:name=".RestaurantsActivity" />
        <activity android:name=".CustomerLoginInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>